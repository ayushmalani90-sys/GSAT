"use client";

import { useCallback, useEffect, useMemo, useState } from "react";

type Quote = { symbol: string; label: string; price: number; changePercent: number | null; provider?: string; providerUpdatedAt?: string | null };
type QuoteResponse = { quotes: Quote[]; updatedAt: string; spotError?: string | null };
type Level = { price: number; strength: number; touches: number; distancePct?: number } | number;
type Analysis = {
  price: number | null; samples: number;
  ema: { ema20: number | null; ema50: number | null; ema200: number | null; priceVsEma20: string; priceVsEma50: string; priceVsEma200: string; bias: string; interpretation?: string };
  momentum: { rsi14: number | null; rsiBias: string; macd: number | null; macdSignal: number | null; macdHistogram: number | null; macdBias: string; interpretation?: string };
  volatility?: { atr14: number | null; atrPercent: number | null; interpretation?: string };
  supportResistance: { supports: Level[]; resistances: Level[]; method?: string };
  fibonacci?: { swingLow: number | null; swingHigh: number | null; levels: { ratio: string; price: number }[]; interpretation?: string };
  volumeProfile?: { poc: number | null; highVolumeNodes: number[]; lowVolumeNodes: number[]; interpretation?: string };
  patterns?: { name: string; direction: "Bullish" | "Bearish" | "Neutral"; confidence: number; description: string }[];
  overall?: { bias: string; summary: string };
};
type AnalysisResponse = { source?: string; generatedAt?: string; timeframe?: string; gold: { intraday: Analysis }; silver: { intraday: Analysis }; methodology?: { note?: string; dataQuality?: string } };

const money=(v:number|null)=>v!=null&&Number.isFinite(v)?v.toLocaleString("en-US",{minimumFractionDigits:2,maximumFractionDigits:2}):"--";
const levelPrice=(l:Level)=>typeof l==="number"?l:l.price;
const levelStrength=(l:Level)=>typeof l==="number"?null:l.strength;
const levelTouches=(l:Level)=>typeof l==="number"?null:l.touches;
function statusFromQuotes(q:Quote[]){const stamps=q.filter(x=>x.providerUpdatedAt).map(x=>Date.parse(x.providerUpdatedAt as string));const latest=stamps.length?Math.max(...stamps):NaN;if(!Number.isFinite(latest))return{label:"DATA STATUS UNKNOWN",detail:"Waiting for spot timestamp"};const age=Math.max(0,Date.now()-latest)/1000;return age<=90?{label:"SPOT DATA FRESH",detail:`Updated ${Math.round(age)}s ago`}:{label:"SPOT DATA STALE",detail:`Last update ${Math.round(age/60)}m ago`};}
function dataTone(value:string){if(/above|bull|buy|positive/i.test(value))return"positive";if(/below|bear|sell|negative/i.test(value))return"negative";return"neutral";}
function TradingViewChart({symbol,title,timeframe}:{symbol:string;title:string;timeframe:string}){useEffect(()=>{const id=`tv_${symbol.replace(/[^a-zA-Z0-9]/g,"_")}`;const container=document.getElementById(id);if(!container)return;container.innerHTML="";const widget=document.createElement("div");widget.className="tradingview-widget-container";widget.style.height="100%";widget.style.width="100%";const chart=document.createElement("div");chart.className="tradingview-widget-container__widget";chart.style.height="100%";chart.style.width="100%";widget.appendChild(chart);container.appendChild(widget);const script=document.createElement("script");script.src="https://s3.tradingview.com/external-embedding/embed-widget-advanced-chart.js";script.type="text/javascript";script.async=true;const intervals:Record<string,string>={"15m":"15","1H":"60","4H":"240","1D":"D"};script.innerHTML=JSON.stringify({autosize:true,symbol,interval:intervals[timeframe]??"60",timezone:"Asia/Kolkata",theme:"dark",style:"1",locale:"en",allow_symbol_change:false,hide_top_toolbar:false,hide_legend:false,hide_side_toolbar:false,hide_volume:false,withdateranges:true,save_image:false,calendar:false,support_host:"https://www.tradingview.com",studies:["RSI@tv-basicstudies","MACD@tv-basicstudies"]});widget.appendChild(script);return()=>{const current=document.getElementById(id);if(current)current.innerHTML="";};},[symbol,timeframe]);return <section className="gsat-card gsat-chart-card"><div className="gsat-card-head"><div><div className="gsat-kicker">{title}</div><div className="gsat-card-subtitle">TradingView spot feed · RSI + MACD · {timeframe}</div></div><span className="gsat-status-dot"><span/> LIVE</span></div><div id={`tv_${symbol.replace(/[^a-zA-Z0-9]/g,"_")}`} className="gsat-chart-frame"/></section>;}
function SpotCard({quote,accent}:{quote:Quote|undefined;accent:"gold"|"silver"}){const change=quote?.changePercent;const tone=change!=null?(change>=0?"positive":"negative"):"neutral";const updated=quote?.providerUpdatedAt?new Date(quote.providerUpdatedAt).toLocaleTimeString([], {hour:"2-digit",minute:"2-digit",second:"2-digit"}):"--";const state=(quote?.provider??"")+"";return <article className={`gsat-card gsat-spot-card ${accent}`}><div className="gsat-spot-top"><div className="gsat-spot-name"><span className={`gsat-metal-mark ${accent}`}>{accent==="gold"?"Au":"Ag"}</span><div><div className="gsat-kicker">{accent==="gold"?"GOLD":"SILVER"}</div><div className="gsat-card-subtitle">USD / troy oz · spot</div></div></div><span className="gsat-live-pill">LIVE</span></div><div className="gsat-spot-price">${money(quote?.price??null)}</div><div className="gsat-spot-bottom"><span className={`gsat-change ${tone}`}>{change==null?"--":`${change>=0?"+":""}${change.toFixed(2)}%`}</span><span className="gsat-card-subtitle">{state||"BiQuote"} · Updated {updated}</span></div></article>;}
function RatioCard({gold,silver}:{gold:Quote|undefined;silver:Quote|undefined}){const ratio=gold&&silver&&silver.price>0?gold.price/silver.price:null;return <article className="gsat-card gsat-spot-card ratio"><div className="gsat-spot-top"><div className="gsat-spot-name"><span className="gsat-metal-mark ratio">R</span><div><div className="gsat-kicker">GOLD / SILVER RATIO</div><div className="gsat-card-subtitle">Relative spot value</div></div></div></div><div className="gsat-spot-price">{ratio==null?"--":ratio.toFixed(2)}×</div><div className="gsat-card-subtitle">Gold spot ÷ silver spot</div></article>;}
function EmaCard({label,value,relation,price}:{label:string;value:number|null;relation:string;price:number|null}){const tone=dataTone(relation);return <div className={`gsat-ema-card ${tone}`}><div className="gsat-ema-head"><span>{label}</span><span className={`gsat-mini-badge ${tone}`}>{relation||"--"}</span></div><div className="gsat-ema-value">{money(value)}</div><div className="gsat-ema-sub">Current price ${money(price)}</div></div>;}
function LevelRow({level,label,kind}:{level:Level;label:string;kind:"support"|"resistance"}){return <div className={`gsat-level-row ${kind}`}><div><div className="gsat-level-label">{label}</div><div className="gsat-level-price">{money(levelPrice(level))}</div></div><div className="gsat-level-meta"><div>{levelStrength(level)!=null?`Strength ${levelStrength(level)}`:"Pivot"}</div><div>{levelTouches(level)!=null?`${levelTouches(level)} touches`:"Confirmed level"}</div></div></div>;}
function AnalysisSection({title,children}:{title:string;children:React.ReactNode}){return <div className="gsat-analysis-section"><div className="gsat-section-label">{title}</div>{children}</div>;}
function TechSupplement({analysis}:{analysis:Analysis}){return <div className="gsat-tech-supplement">
  <AnalysisSection title="MOMENTUM"><div className="gsat-momentum-grid"><div className="gsat-momentum-card"><div className="gsat-section-label">RSI 14</div><div className="gsat-momentum-value">{analysis.momentum.rsi14==null?"--":analysis.momentum.rsi14.toFixed(2)}</div><div className={`gsat-momentum-badge ${dataTone(analysis.momentum.rsiBias)}`}>{analysis.momentum.rsiBias}</div></div><div className="gsat-momentum-card"><div className="gsat-section-label">MACD 12 / 26 / 9</div><div className="gsat-momentum-value">{analysis.momentum.macd==null?"--":analysis.momentum.macd.toFixed(4)}</div><div className={`gsat-momentum-badge ${dataTone(analysis.momentum.macdBias)}`}>{analysis.momentum.macdBias}</div><div className="gsat-card-subtitle">Signal {analysis.momentum.macdSignal==null?"--":analysis.momentum.macdSignal.toFixed(4)} · Hist {analysis.momentum.macdHistogram==null?"--":analysis.momentum.macdHistogram.toFixed(4)}</div></div></div></AnalysisSection>
  <AnalysisSection title="VOLATILITY & LEVELS"><div className="gsat-analysis-grid">{analysis.volatility&&<div className="gsat-subcard"><div className="gsat-section-label">ATR 14 · VOLATILITY</div><div className="gsat-subcard-value">{money(analysis.volatility.atr14)}</div><div className="gsat-card-subtitle">{analysis.volatility.atrPercent==null?"--":`${analysis.volatility.atrPercent.toFixed(2)}% of price`}</div><div className="gsat-note">{analysis.volatility.interpretation}</div></div>}{analysis.fibonacci&&<div className="gsat-subcard"><div className="gsat-section-label">FIBONACCI RETRACEMENT</div><div className="gsat-card-subtitle">Swing {money(analysis.fibonacci.swingLow)} → {money(analysis.fibonacci.swingHigh)}</div>{analysis.fibonacci.levels.map(x=><div className="gsat-inline-row" key={x.ratio}><span>{x.ratio}</span><strong>{money(x.price)}</strong></div>)}<div className="gsat-note">{analysis.fibonacci.interpretation}</div></div>}</div></AnalysisSection>
  <AnalysisSection title="VOLUME & PATTERN"><div className="gsat-analysis-grid">{analysis.volumeProfile&&<div className="gsat-subcard"><div className="gsat-section-label">VOLUME PROFILE</div><div className="gsat-subcard-value">POC {money(analysis.volumeProfile.poc)}</div><div className="gsat-card-subtitle">HVN {analysis.volumeProfile.highVolumeNodes.map(x=>money(x)).join(" · ")||"--"}</div><div className="gsat-card-subtitle">LVN {analysis.volumeProfile.lowVolumeNodes.map(x=>money(x)).join(" · ")||"--"}</div><div className="gsat-note">{analysis.volumeProfile.interpretation}</div></div>}{analysis.patterns?.length?<div className="gsat-subcard"><div className="gsat-section-label">PATTERN DETECTION</div><div className="gsat-pattern-list">{analysis.patterns.map((p,i)=><div className="gsat-pattern-row" key={`${p.name}-${i}`}><div><strong>{p.name}</strong><div className="gsat-card-subtitle">{p.description}</div></div><span className={`gsat-bias ${dataTone(p.direction)}`}>{p.direction} · {p.confidence}%</span></div>)}</div></div>:null}</div></AnalysisSection>
</div>}
function TechnicalCard({metal,analysis,timeframe}:{metal:string;analysis:Analysis|null;timeframe:string}){if(!analysis)return <article className="gsat-card gsat-tech-card"><div className="gsat-kicker">{metal.toUpperCase()} ANALYSIS</div><div className="gsat-loading">Loading technical analysis…</div></article>;const bias=analysis.overall?.bias||analysis.ema.bias||"Mixed";const supports=analysis.supportResistance.supports.slice(0,3);const resistances=analysis.supportResistance.resistances.slice(0,3);return <article className="gsat-card gsat-tech-card"><div className="gsat-card-head"><div><div className="gsat-kicker">{metal.toUpperCase()} TECHNICAL ANALYSIS</div><div className="gsat-card-subtitle">{timeframe} · complete technical dashboard</div></div><span className={`gsat-bias ${dataTone(bias)}`}>{bias}</span></div><AnalysisSection title="TREND · EMA"><div className="gsat-ema-grid"><EmaCard label="EMA 20" value={analysis.ema.ema20} relation={analysis.ema.priceVsEma20} price={analysis.price}/><EmaCard label="EMA 50" value={analysis.ema.ema50} relation={analysis.ema.priceVsEma50} price={analysis.price}/><EmaCard label="EMA 200" value={analysis.ema.ema200} relation={analysis.ema.priceVsEma200} price={analysis.price}/></div>{analysis.ema.interpretation?<div className="gsat-note">{analysis.ema.interpretation}</div>:null}</AnalysisSection><AnalysisSection title="STRUCTURE · SUPPORT / RESISTANCE"><div className="gsat-level-columns"><div><div className="gsat-section-label support">SUPPORT</div><div className="gsat-level-list">{supports.length?supports.map((level,i)=><LevelRow key={`s-${i}-${levelPrice(level)}`} level={level} label={`S${i+1}`} kind="support"/>):<div className="gsat-empty">No confirmed support</div>}</div></div><div><div className="gsat-section-label resistance">RESISTANCE</div><div className="gsat-level-list">{resistances.length?resistances.map((level,i)=><LevelRow key={`r-${i}-${levelPrice(level)}`} level={level} label={`R${i+1}`} kind="resistance"/>):<div className="gsat-empty">No confirmed resistance</div>}</div></div></div></AnalysisSection><TechSupplement analysis={analysis}/></article>;}
export default function Home(){const [data,setData]=useState<QuoteResponse>({quotes:[],updatedAt:""});const [analysis,setAnalysis]=useState<AnalysisResponse|null>(null);const [timeframe,setTimeframe]=useState("1H");const [loading,setLoading]=useState(true);const [error,setError]=useState("");const load=useCallback(async(tf=timeframe)=>{try{setLoading(true);setError("");const q=await fetch(`/api/quotes?fresh=${Date.now()}`,{cache:"no-store"});if(!q.ok)throw new Error("Live spot request failed");setData(await q.json());const a=await fetch(`/api/analysis?timeframe=${encodeURIComponent(tf)}&fresh=${Date.now()}`,{cache:"no-store"});const aData=await a.json();if(!a.ok)throw new Error(aData?.error||"Technical analysis unavailable");setAnalysis(aData);}catch(e){setError(e instanceof Error?e.message:"Live market data unavailable");}finally{setLoading(false);}},[timeframe]);useEffect(()=>{void load(timeframe);const timer=window.setInterval(()=>void load(timeframe),60000);return()=>window.clearInterval(timer);},[load,timeframe]);const status=useMemo(()=>statusFromQuotes(data.quotes),[data.quotes]);const gold=data.quotes.find(q=>q.label==="Gold");const silver=data.quotes.find(q=>q.label==="Silver");const goldAnalysis=analysis?.gold.intraday??null;const silverAnalysis=analysis?.silver.intraday??null;const timeframes=["15m","1H","4H","1D"];return <main className="gsat-shell"><header className="gsat-header"><div><div className="gsat-brand-row"><span className="gsat-brand">GSAT</span><span className="gsat-status-dot"><span/> LIVE</span></div><h1>Gold &amp; Silver Analysis Terminal</h1><p>Live spot prices and systematic technical analysis.</p></div><div className="gsat-header-right"><div className="gsat-update">{status.detail}</div><button className="gsat-refresh" onClick={()=>void load(timeframe)}>{loading?"Refreshing…":"Refresh"}</button></div></header>{error?<div className="gsat-error">{error}</div>:null}<section className="gsat-section"><div className="gsat-section-heading"><div><div className="gsat-kicker">MARKET SNAPSHOT</div><h2>Spot Prices</h2></div><div className="gsat-card-subtitle">USD per troy ounce</div></div><div className="gsat-grid-3"><SpotCard quote={gold} accent="gold"/><SpotCard quote={silver} accent="silver"/><RatioCard gold={gold} silver={silver}/></div></section><section className="gsat-section"><div className="gsat-section-heading"><div><div className="gsat-kicker">MARKET CHARTS</div><h2>TradingView</h2></div><div className="gsat-card-subtitle">{timeframe} · spot · RSI + MACD</div></div><div className="gsat-grid-2"><TradingViewChart symbol="OANDA:XAUUSD" title="GOLD · XAU/USD" timeframe={timeframe}/><TradingViewChart symbol="OANDA:XAGUSD" title="SILVER · XAG/USD" timeframe={timeframe}/></div></section><section className="gsat-section"><div className="gsat-section-heading"><div><div className="gsat-kicker">TECHNICAL ENGINE</div><h2>Technical Analysis</h2></div><div className="gsat-card-subtitle">Systematic multi-indicator view</div></div><div className="gsat-timeframes">{timeframes.map(tf=><button key={tf} onClick={()=>setTimeframe(tf)} className={`gsat-timeframe ${tf===timeframe?"active":""}`}>{tf}</button>)}</div><div className="gsat-grid-2"><TechnicalCard metal="Gold" analysis={goldAnalysis} timeframe={timeframe}/><TechnicalCard metal="Silver" analysis={silverAnalysis} timeframe={timeframe}/></div></section><footer className="gsat-footer">GSAT · Spot prices via BiQuote · Technical analysis via GSAT OHLC engine</footer></main>;}
