"use client";

import { useCallback, useEffect, useMemo, useState } from "react";

type Quote = { symbol: string; label: string; price: number; changePercent: number | null; provider?: string; providerUpdatedAt?: string | null };
type QuoteResponse = { quotes: Quote[]; updatedAt: string; spotError?: string | null };
type Analysis = { price: number | null; samples: number; ema: { ema20: number | null; ema50: number | null; ema200: number | null; priceVsEma20: string; priceVsEma50: string; priceVsEma200: string; bias: string }; momentum: { rsi14: number | null; macd: number | null }; supportResistance: { supports: number[]; resistances: number[] } };
type AnalysisResponse = { source: string; generatedAt: string; gold: { intraday: Analysis; daily: Analysis }; silver: { intraday: Analysis; daily: Analysis }; methodology?: { note?: string } };

const money = (value: number | null) => value != null && Number.isFinite(value) ? value.toLocaleString("en-US", { minimumFractionDigits: 2, maximumFractionDigits: 2 }) : "--";

function statusFromQuotes(quotes: Quote[]) {
  const timestamps = quotes.filter((q) => q.providerUpdatedAt).map((q) => Date.parse(q.providerUpdatedAt as string));
  const latest = timestamps.length ? Math.max(...timestamps) : NaN;
  if (!Number.isFinite(latest)) return { label: "DATA STATUS UNKNOWN", detail: "Waiting for spot timestamp" };
  const ageSeconds = Math.max(0, Date.now() - latest) / 1000;
  return ageSeconds <= 90 ? { label: "SPOT DATA FRESH", detail: `Updated ${Math.round(ageSeconds)}s ago` } : { label: "SPOT DATA STALE", detail: `Last update ${Math.round(ageSeconds / 60)}m ago` };
}

function TradingViewChart({ symbol, title }: { symbol: string; title: string }) {
  useEffect(() => {
    const id = `tv_${symbol.replace(/[^a-zA-Z0-9]/g, "_")}`;
    const container = document.getElementById(id);
    if (!container) return;
    container.innerHTML = "";
    const widget = document.createElement("div"); widget.className = "tradingview-widget-container"; widget.style.height = "100%"; widget.style.width = "100%";
    const chart = document.createElement("div"); chart.className = "tradingview-widget-container__widget"; chart.style.height = "calc(100% - 32px)"; chart.style.width = "100%";
    widget.appendChild(chart); container.appendChild(widget);
    const script = document.createElement("script"); script.src = "https://s3.tradingview.com/external-embedding/embed-widget-advanced-chart.js"; script.type = "text/javascript"; script.async = true;
    script.innerHTML = JSON.stringify({ autosize: true, symbol, interval: "60", timezone: "Asia/Kolkata", theme: "dark", style: "1", locale: "en", allow_symbol_change: false, hide_top_toolbar: false, hide_legend: false, hide_side_toolbar: false, hide_volume: false, withdateranges: true, save_image: false, calendar: false, support_host: "https://www.tradingview.com", studies: ["RSI@tv-basicstudies", "MACD@tv-basicstudies"] });
    widget.appendChild(script);
    return () => { const current = document.getElementById(id); if (current) current.innerHTML = ""; };
  }, [symbol]);
  return <article className="overflow-hidden rounded-2xl border border-zinc-800 bg-[#0d0d0d]"><div className="border-b border-zinc-800 px-4 py-3"><p className="text-[10px] font-semibold tracking-[0.2em] text-zinc-500">{title}</p><p className="mt-1 text-xs text-zinc-600">TradingView • OANDA spot feed • RSI + MACD</p></div><div id={`tv_${symbol.replace(/[^a-zA-Z0-9]/g, "_")}`} className="h-[640px] w-full" /></article>;
}

function EmaCard({ name, value, price }: { name: string; value: number | null; price: number | null }) {
  const relation = value == null || price == null ? "Unavailable" : price > value ? "Price above EMA" : "Price below EMA";
  return <article className="rounded-xl border border-zinc-800 bg-black/20 p-4"><div className="flex items-center justify-between"><h3 className="text-sm font-semibold text-zinc-200">{name}</h3><span className={relation === "Price above EMA" ? "text-xs text-emerald-400" : relation === "Price below EMA" ? "text-xs text-red-400" : "text-xs text-zinc-600"}>{relation}</span></div><p className="mt-4 font-mono text-2xl font-semibold">{money(value)}</p><p className="mt-2 text-[11px] text-zinc-600">Live price: {money(price)}</p></article>;
}

function TechnicalPanel({ metal, analysis }: { metal: string; analysis: Analysis | null }) {
  if (!analysis) return <section className="rounded-2xl border border-zinc-800 bg-[#0d0d0d] p-5">Loading technical analysis…</section>;
  const rsi = analysis.momentum.rsi14;
  const rsiText = rsi == null ? "Unavailable" : rsi >= 70 ? "Overbought" : rsi <= 30 ? "Oversold" : rsi >= 50 ? "Bullish momentum" : "Bearish momentum";
  return <section className="rounded-2xl border border-zinc-800 bg-[#0d0d0d] p-5 space-y-5"><div><p className="text-xs font-semibold tracking-[0.2em] text-zinc-500">{metal.toUpperCase()} TECHNICAL ANALYSIS</p><div className="mt-1 flex items-end justify-between gap-4"><h2 className="text-xl font-semibold">EMA + Support / Resistance</h2><span className="rounded-full border border-zinc-800 px-3 py-1 text-xs text-amber-300">{analysis.ema.bias}</span></div></div><div className="grid gap-3 md:grid-cols-3"><EmaCard name="EMA 20" value={analysis.ema.ema20} price={analysis.price}/><EmaCard name="EMA 50" value={analysis.ema.ema50} price={analysis.price}/><EmaCard name="EMA 200" value={analysis.ema.ema200} price={analysis.price}/></div><div className="grid gap-3 md:grid-cols-2"><div className="rounded-xl border border-zinc-800 bg-black/20 p-4"><p className="text-xs tracking-[0.16em] text-zinc-600">SUPPORT</p><div className="mt-3 grid grid-cols-3 gap-2">{analysis.supportResistance.supports.length ? analysis.supportResistance.supports.map((v) => <div key={v} className="rounded-lg border border-emerald-500/10 bg-emerald-500/5 p-3 text-center font-mono text-sm text-emerald-300">{money(v)}</div>) : <p className="text-sm text-zinc-600">No confirmed support in current lookback</p>}</div></div><div className="rounded-xl border border-zinc-800 bg-black/20 p-4"><p className="text-xs tracking-[0.16em] text-zinc-600">RESISTANCE</p><div className="mt-3 grid grid-cols-3 gap-2">{analysis.supportResistance.resistances.length ? analysis.supportResistance.resistances.map((v) => <div key={v} className="rounded-lg border border-red-500/10 bg-red-500/5 p-3 text-center font-mono text-sm text-red-300">{money(v)}</div>) : <p className="text-sm text-zinc-600">No confirmed resistance in current lookback</p>}</div></div></div><div className="grid gap-3 md:grid-cols-3"><div className="rounded-xl border border-zinc-800 bg-black/20 p-4"><p className="text-xs text-zinc-600">RSI 14</p><p className="mt-2 font-mono text-2xl">{rsi == null ? "--" : rsi.toFixed(2)}</p><p className="mt-1 text-xs text-amber-300">{rsiText}</p></div><div className="rounded-xl border border-zinc-800 bg-black/20 p-4"><p className="text-xs text-zinc-600">MACD</p><p className="mt-2 font-mono text-2xl">{analysis.momentum.macd == null ? "--" : analysis.momentum.macd.toFixed(4)}</p><p className="mt-1 text-xs text-zinc-500">Momentum differential</p></div><div className="rounded-xl border border-zinc-800 bg-black/20 p-4"><p className="text-xs text-zinc-600">Samples</p><p className="mt-2 font-mono text-2xl">{analysis.samples}</p><p className="mt-1 text-xs text-zinc-500">Source observations used</p></div></div></section>;
}

export default function Home() {
  const [data, setData] = useState<QuoteResponse>({ quotes: [], updatedAt: "" });
  const [analysis, setAnalysis] = useState<AnalysisResponse | null>(null);
  const [loading, setLoading] = useState(true);
  const [error, setError] = useState("");
  const load = useCallback(async () => { try { setLoading(true); setError(""); const [q, a] = await Promise.all([fetch(`/api/quotes?ts=${Date.now()}`, { cache: "no-store" }), fetch(`/api/analysis?hours=48&ts=${Date.now()}`, { cache: "no-store" })]); if (!q.ok || !a.ok) throw new Error("Live market analysis request failed"); setData(await q.json()); setAnalysis(await a.json()); } catch (e) { setError(e instanceof Error ? e.message : "Live analysis unavailable"); } finally { setLoading(false); } }, []);
  useEffect(() => { void load(); const timer = window.setInterval(() => void load(), 60000); return () => window.clearInterval(timer); }, [load]);
  const status = useMemo(() => statusFromQuotes(data.quotes), [data.quotes]); const gold = data.quotes.find((q) => q.label === "Gold"); const silver = data.quotes.find((q) => q.label === "Silver");
  return <main className="min-h-screen bg-[#070707] px-4 py-5 text-zinc-100 md:px-6 lg:px-8"><div className="mx-auto max-w-[1550px] space-y-5"><header className="rounded-2xl border border-zinc-800 bg-[#0d0d0d] p-6"><div className="flex flex-col gap-4 lg:flex-row lg:items-end lg:justify-between"><div><p className="text-xs font-bold tracking-[0.32em] text-amber-300">GSAT</p><h1 className="mt-2 text-3xl font-semibold tracking-tight md:text-4xl">Gold & Silver Analysis Terminal</h1><p className="mt-2 text-sm text-zinc-500">XAUS spot prices • TradingView charts • live technical analysis</p></div><div className="flex flex-wrap items-center gap-3"><span className="rounded-full border border-zinc-800 bg-black/30 px-3 py-2 text-xs text-zinc-400">{status.label}</span><span className="rounded-full border border-zinc-800 bg-black/30 px-3 py-2 text-xs text-zinc-500">{status.detail}</span><button onClick={() => void load()} className="rounded-xl border border-amber-300/20 bg-amber-300/10 px-4 py-2 text-sm font-medium text-amber-200">{loading ? "Refreshing…" : "Refresh"}</button></div></div></header><section className="grid gap-4 md:grid-cols-2"><article className="rounded-2xl border border-zinc-800 bg-[#0d0d0d] p-5"><p className="text-xs font-semibold tracking-[0.2em] text-zinc-500">GOLD SPOT / USD</p><p className="mt-4 font-mono text-4xl font-semibold">{money(gold?.price ?? null)}</p><p className="mt-2 text-xs text-zinc-600">XAUS • {gold?.providerUpdatedAt ? new Date(gold.providerUpdatedAt).toLocaleTimeString("en-IN") : "--"}</p></article><article className="rounded-2xl border border-zinc-800 bg-[#0d0d0d] p-5"><p className="text-xs font-semibold tracking-[0.2em] text-zinc-500">SILVER SPOT / USD</p><p className="mt-4 font-mono text-4xl font-semibold">{money(silver?.price ?? null)}</p><p className="mt-2 text-xs text-zinc-600">XAUS • {silver?.providerUpdatedAt ? new Date(silver.providerUpdatedAt).toLocaleTimeString("en-IN") : "--"}</p></article></section><section className="grid gap-5 xl:grid-cols-2"><TradingViewChart symbol="OANDA:XAUUSD" title="GOLD SPOT / USD"/><TradingViewChart symbol="OANDA:XAGUSD" title="SILVER SPOT / USD"/></section><section className="grid gap-5 xl:grid-cols-2"><TechnicalPanel metal="Gold" analysis={analysis?.gold.intraday ?? null}/><TechnicalPanel metal="Silver" analysis={analysis?.silver.intraday ?? null}/></section><section className="rounded-2xl border border-zinc-800 bg-[#0d0d0d] p-5"><p className="text-xs font-semibold tracking-[0.2em] text-zinc-500">ANALYSIS ENGINE</p><h2 className="mt-1 text-xl font-semibold">Price → EMA structure → Momentum → Support / Resistance</h2><p className="mt-2 text-sm text-zinc-600">GSAT now calculates EMA 20/50/200, RSI, MACD and local swing-based support/resistance from XAUS source series. The engine refreshes with the spot feed.</p>{analysis?.methodology?.note && <p className="mt-3 text-xs text-zinc-700">{analysis.methodology.note}</p>}{(error || data.spotError) && <p className="mt-5 rounded-xl border border-red-500/20 bg-red-500/5 p-3 text-xs text-red-300">{error || data.spotError}</p>}</section><footer className="border-t border-zinc-900 pt-3 text-[10px] tracking-[0.14em] text-zinc-600">GSAT • SPOT: XAUS • CHARTS: TRADINGVIEW/OANDA</footer></div></main>;
}
